# JokerLand Landing

Esta rama incluye una landing page estática en español.

## Archivos
- `index.html`
- `styles.css`

## Vista local
```bash
python3 -m http.server 8000
```
Luego abre: `http://localhost:8000`

## Nota sobre GitHub
Si no ves estos archivos en tu repo de GitHub, normalmente significa que:
1. Estás mirando otra rama (por ejemplo `main`), o
2. Los cambios no se han subido/mezclado en esa rama.

En esta copia de trabajo, los archivos sí existen en la rama `work`.
